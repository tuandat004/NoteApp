<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NoteApp"
        tools:targetApi="31">
        <activity
            android:name=".Splash.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AccountService.View.ForgotPasswordActivity"
            android:exported="false" />
        <activity
            android:name=".AccountService.View.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".AccountService.View.LoginActivity"
            android:exported="false"/>
        <activity
            android:name=".UserService.View.UserProfileActivity"
            android:exported="false"/>
        <activity
            android:name=".MainActivity"
            android:exported="false" /> <!-- NoteService -->
        <activity
            android:name=".NoteService.View.NoteHomeActivity"
            android:exported="false" />
        <activity
            android:name=".NoteService.View.CreateNoteActivity"
            android:windowSoftInputMode="adjustResize"
            android:exported="false" />
        <activity
            android:name=".NoteService.View.NoteDetailActivity"
            android:windowSoftInputMode="adjustResize"
            android:exported="false" /> 

        <activity
            android:name=".NoteService.View.TrashActivity"
            android:exported="false" />

        <receiver
            android:name=".ReminderService.Worker.ReminderReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- TagService -->
        <activity
            android:name=".TagService.View.ManageTagsActivity"
            android:exported="false" /> <!-- SearchService -->
        <activity
            android:name=".SearchService.View.SearchActivity"
            android:exported="false" />

    </application>

</manifest>